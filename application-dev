eureka:
  instance:
    hostname: localhost
  client:
    fetch-registry: false
    register-with-eureka: false

  
spring:
  application:
    name: CONFIG-DEV
    
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Akankshaanpat/microservice-tutorial-config
          clone-on-start: true
